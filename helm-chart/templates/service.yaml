apiVersion: v1
kind: Service
metadata:
  name: {{ include "openwebui-chatbot.fullname" . }}
  labels:
    {{- include "openwebui-chatbot.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "openwebui-chatbot.selectorLabels" . | nindent 4 }}
